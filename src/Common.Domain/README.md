# 📄 Client Domain Pattern

A reference implementation for a common `Client` entity used in many systems.

### Includes:

- `Client.cs` – the domain model
- `IClientRepository`, `ClientRepository`
- `IClientService`, `ClientService`

Use this as a scaffold to create consistent domain modules.
