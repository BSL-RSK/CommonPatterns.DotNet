# 💡 Source Code (src)

This folder contains reusable class libraries and domain abstractions.

### Submodules:

- [`Common.Database`](Common.Database/README.md): Database-level patterns (SQL connection, repository base)
- [`Common.Domain`](Common.Domain/README.md): Domain logic examples, starting with the `Client` domain

Use these modules as drop-in components in other .NET applications.
