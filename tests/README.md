# ✅ Tests

This folder contains unit test templates and common test strategies.

### Projects:

- [`Common.Tests`](Common.Tests/README.md): General reusable test logic, templates, and helpers

All tests use `xUnit` and `Moq` (or similar) for mocking and assertions.
