# 🏋️‍️ Design Guidelines

- Prefer constructor injection
- Keep repositories focused on one entity
- Keep services free of persistence logic
- Return meaningful error types or `Result<T>` patterns where needed
- Keep `main` clean and protected – use `develop` and feature branches
